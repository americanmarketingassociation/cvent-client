export interface CventConfig {
  data: string[];
  cventData:   string[],
  attendee_id: string[],
  attendeeData: string[],
};

export interface CVentClientCredentials {
  CVENT_CLIENT_ID: string;
  CVENT_CLIENT_SECRET: string;
}